# Example of .env file, rename to .env and change values as appropriate
# Number of requests to buffer before inserting to bigquery
BIGQUERY_BUFFER_SIZE = 100
# The secret name for the APIKEY to send to AISStream
AISSTREAM_APIKEY_SECRET_NAME = "projects/<GOOGLE PROJECT NUMBER>/secrets/<AISSTREAM API KEY SECRET NAME>/versions/latest"
# The Config JSON
CONFIG_BUCKET = "<BUCKET NAME>" # Omit for local path
CONFIG_PATH = "config/ais_listener.conf.json" 
